#! ./build/scriptism
#
# Some scenarios that use interpolated strings.
#

var s1 = "one"
var s2 = "#{s1}"

if s1 == s2 {
    println "Interpolations are equal."
} else {
    println "ERROR: Interpolations should be equal but they are not."
    println "s1 = '#{s1}'"
    println "s2 = '#{s2}'"
}
